# frozen_string_literal: true
source 'https://rubygems.org'

ruby '~> 2.5.0'

gem 'arduino_firmata'
gem 'colorize'
gem 'dotenv'

group :test do
  gem 'byebug'
  gem 'minitest'
  gem 'simplecov', require: false
  gem 'webmock'
end
